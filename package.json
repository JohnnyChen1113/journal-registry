{
  "name": "journal-registry",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Auto-maintained registry of scientific journals (domain + medicine).",
  "license": "CC0-1.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.js"
  }
}
